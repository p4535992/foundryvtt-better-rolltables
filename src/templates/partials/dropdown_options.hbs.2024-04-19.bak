{{#each data as |array group|}}
    <optgroup label="{{group}}">
    {{#each array}}
        <option value="{{this.uuidDoc}}" data-label="{{this.name}}">{{this.name}}</option>
    {{/each}}
    </optgroup>
{{/each}}